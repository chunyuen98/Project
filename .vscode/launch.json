{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "pwa-chrome",
            "request": "launch",
            "name": "Launch Login Page",
            "file": "${workspaceFolder}/Login/login.html"
        },
        {
            "type": "pwa-chrome",
            "request": "launch",
            "name": "Launch Signup Page",
            "file": "${workspaceFolder}/Login/signup.html"
        }
    ]
}
