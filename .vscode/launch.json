{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "pwa-chrome",
            "request": "launch",
            "name": "Launch Login Page",
            "file": "${workspaceFolder}/index.html"
        },
        {
            "type": "pwa-chrome",
            "request": "launch",
            "name": "Launch Signup Page",
            "file": "${workspaceFolder}/index.html"
        }
    ]
}
